import { HubRpcClient } from '@farcaster/hub-nodejs';
import { FrameRequest, FrameValidationResponse } from './farcasterTypes';
export declare function getHubClient(): HubRpcClient;
/**
 * Given a frame message, decode and validate it.
 *
 * If message is valid, return the message. Otherwise undefined.
 *
 * @param body The JSON received by server on frame callback
 */
declare function getFrameMessage(body: FrameRequest): Promise<FrameValidationResponse>;
export { getFrameMessage };
